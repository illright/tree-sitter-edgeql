# link01
# <- comment

abstract link todo {
# <- keyword
	property rank -> int64 {
	# <- keyword
	#        ^ property
	#             ^ operator
	#                ^ type.builtin
		default := 42;
		# <- keyword
		#          ^ number
	}
}
